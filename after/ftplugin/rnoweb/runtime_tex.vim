" inherit the tex custom files, such as the 
"  the arpeggio mappings mk -> $ $, dm -> \[ \]
"  spacing within $ $, \[ \], \{ \}
"  sandwich recipes
"  vimtex mappings

runtime ftplugin/tex/pairs_arpeggio_tex.vim
runtime ftplugin/tex/pairspace_tex.vim
runtime ftplugin/tex/sandwich_recipes.vim
runtime ftplugin/tex/vimtex_mappings.vim
runtime ftplugin/tex/vimtex_leader_mappings.vim

" a workaround until one can associate mixed filetypes within a single file 
" upd. nov 2023

